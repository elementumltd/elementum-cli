# Organization queries

query GetOrganization {
  organization {
    id
    name
  }
}

query GetCategories {
  organization {
    categories {
      edges {
        node {
          id
          name
        }
      }
    }
  }
}
